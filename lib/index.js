const {ValidateColor, colorize, hexToRGB, joinColors, rgb, rgba} = require('./functions.js');
const {FONTS,BACKGROUND,FOREGROUND} = require('./constants.js');
module.exports = {
  FONTS, BACKGROUND, FOREGROUND,
  ValidateColor, colorize, hexToRGB, joinColors, rgb, rgba
}
